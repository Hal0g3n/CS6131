const config = {
    db: {
        /* don't expose password or any sensitive info, done only for demo */
        host: "db4free.net",
        user: "halogen",
        password: "Pearson1",
        database: "Pearson1",
    },
    listPerPage: 10,
};
module.exports = config;